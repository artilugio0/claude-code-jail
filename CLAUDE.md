# CLAUDE.md

- Do not run git commands (commit, stage, push, etc.) unless explicitly asked.
- When a task is finished, suggest a commit message that summarizes the work done.
- Make only the changes directly requested. No refactoring, extra comments, or unrequested features.
